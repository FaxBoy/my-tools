# 乐刻门店等级查询（H5）

纯静态单页，`index.html` + `leoao-stores-public.json`，无构建。

源码位置：本仓库 **`my-tools/leoao-store-query/`**（与 `my-tools/index.html` 同级）。

数据：在若依管理端「乐刻门店」导出 **导出H5查询数据(JSON)**，将 `leoao-stores-public.json` 放到本目录替换；部署时与 `index.html` 一并发布。

勿用 `file://` 双击打开；请 HTTPS 访问或使用页内「从本机选择 JSON」。
